///////////////////////////////////////////////////////
// 構造体宣言ブロック
///////////////////////////////////////////////////////
struct vertexInput
{
    float4 pos : POSITION0;
    float4 col : COLOR0;
};

struct vertexOutput
{
    float4 pos : SV_POSITION;
    float4 col : COLOR0;
};

///////////////////////////////////////////////////////
// 頂点シェーダプログラムブロック
///////////////////////////////////////////////////////
vertexOutput vs_main(vertexInput inData)
{
    return inData;
}
