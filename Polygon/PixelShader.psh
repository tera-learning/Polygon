///////////////////////////////////////////////////////
// 構造体宣言ブロック
///////////////////////////////////////////////////////
struct pixelInput
{
    float4 pos : SV_POSITION;
	float4 col : COLOR0;
};

///////////////////////////////////////////////////////
// ピクセルシェーダプログラムブロック
///////////////////////////////////////////////////////
float4 ps_main(pixelInput inData) : SV_Target
{
	// 色をそのまま使用
	return inData.col;	
}